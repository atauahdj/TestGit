using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SeventoTwo : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        for(int a = 7; a > 1; a--)
        {
            Debug.Log(a);
        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
