using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OnetoFive : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        for (int a = 1; a < 6; ++a)
        {
            Debug.Log(a);
        }
    }

 
}
